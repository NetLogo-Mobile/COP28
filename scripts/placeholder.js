/** Placeholders: To improve the performance, we intentionally transforms some libraries into placesholders without touching upon NetLogo Web's core simulation features. */
CodeMirror = {
    defineMode: () => {}
}